[project]
name = "backend"
version = "0.1.0"
description = "WealthOS backend API"
readme = "README.md"
authors = [
    { name = "Guna"}
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
]

[project.scripts]
backend = "backend:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
